# Drive Runtime Plugins

`plugins/` is reserved for Drive product/runtime plugin source packages,
marketplace plugin implementations, and extension packages.

Repository/application agent plugins belong under `.sdkwork/plugins/`, not
here.
