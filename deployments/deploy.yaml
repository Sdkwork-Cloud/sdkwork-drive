version: 1
defaultProfile: cloud.production

profiles:
  cloud.production:
    install:
      layout: binary-package
    expose:
      - domain: drive.sdkwork.com
        tls: sdkwork.com
        mode: web+api
        web: pc
        apiPathStyle: full-prefix
    packages:
      - desktop-windows
      - desktop-macos
      - desktop-linux
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: cloud.production
        env: configs/topology/cloud.production.env
      kubernetes:
        reference: deployments/kubernetes/
      proxy:
        upstreams:
          application.public-ingress: http://127.0.0.1:3900
          platform.api-gateway: http://127.0.0.1:3900
      nginx:
        clientMaxBodySize: 32m

  cloud.development:
    install:
      layout: source-tree
    expose:
      - domain: localhost
        tls: off
        mode: web+api
        web: pc
        apiPathStyle: full-prefix
    packages: []
    overrides:
      allowInsecureTls: true
      topology:
        spec: specs/topology.spec.json
        profile: cloud.development
        env: configs/topology/cloud.development.env
      proxy:
        upstreams:
          application.public-ingress: http://127.0.0.1:3900
          platform.api-gateway: http://127.0.0.1:3900

  standalone.production:
    install:
      layout: binary-package
    expose:
      - domain: drive.sdkwork.com
        tls: sdkwork.com
        mode: web+api
        web: pc
        apiPathStyle: full-prefix
    packages:
      - desktop-windows
      - desktop-macos
      - desktop-linux
    overrides:
      topology:
        spec: specs/topology.spec.json
        profile: standalone.production
        env: configs/topology/standalone.production.env
      proxy:
        upstreams:
          application.public-ingress: http://127.0.0.1:3900
          platform.api-gateway: http://127.0.0.1:3900
      nginx:
        clientMaxBodySize: 32m

  standalone.development:
    install:
      layout: source-tree
    expose:
      - domain: localhost
        tls: off
        mode: web+api
        web: pc
        apiPathStyle: full-prefix
    packages: []
    overrides:
      allowInsecureTls: true
      topology:
        spec: specs/topology.spec.json
        profile: standalone.development
        env: configs/topology/standalone.development.env
      proxy:
        upstreams:
          application.public-ingress: http://127.0.0.1:3900
          platform.api-gateway: http://127.0.0.1:3900
