# Applications

`apps/` contains independently runnable application roots for `sdkwork-drive`.

Current layout:

- `sdkwork-drive-pc/`: PC browser and desktop application for Drive file, transfer, storage, and sharing workflows.

See `apps/sdkwork-drive-pc/README.md` for PC application details.
